const express = require('express');
const path = require('path');
const router = require('./routes');
const db = require('../db');

const app = express();

// define routes here


module.exports = app;